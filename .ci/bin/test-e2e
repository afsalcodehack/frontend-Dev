#!/bin/sh

set -e -x

wwwroot="$1"
if [ "$1" ]; then
    wwwroot="$1"
else
    wwwroot=www
fi

if [ ! -d "$wwwroot" ]; then
    echo 'Directory "$wwwroot" not found.'
    exit 1
fi

http-server "$wwwroot" -p 8100 &

sleep 10

exec npm run e2e
